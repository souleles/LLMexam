# Explain Failed Regex Checkpoints with LLM Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Let a professor click a button on a graded submission to get a one-sentence Greek LLM explanation for each checkpoint that failed regex grading.

**Architecture:** A new Python FastAPI endpoint (`/explain-regex-failures`) takes failed checkpoints (id/description/pattern) plus the submission's files, calls `gpt-4o-mini` once (chunked like existing LLM grading) to produce one explanation sentence per checkpoint, and returns them. A new NestJS endpoint (`POST /api/submissions/:id/explain-regex-failures`) loads the submission/exercise/checkpoint state, calls Python, and persists each explanation onto `CheckpointResult.regexFailureExplanation`. The frontend adds a button + modal to `SubmissionDetail` that calls this endpoint and displays the results.

**Tech Stack:** FastAPI + LangChain (`langchain_openai.ChatOpenAI`) + Pydantic on the Python side; NestJS + Prisma + axios (`HttpService`) on the backend; React + Chakra UI + React Query on the frontend.

## Global Constraints

- Greek-language UI text and LLM-facing prompts throughout (matches existing convention).
- `gpt-4o-mini`, `temperature=0.2`, JSON response mode for the new LLM call (per spec — lighter task than full grading, which stays on `gpt-4o`).
- Feature applies only to `EXERCISE`-type submissions with an existing regex grading result containing at least one `matched === false` checkpoint; `PROJECT`-type exercises are out of scope and must be rejected with a 400.
- Explanations always regenerate on each click and overwrite any prior value — no caching/skip-if-exists logic.
- Follow existing patterns exactly: Python services are thin pure-logic + one async LLM-calling function (see `llm_grading_service.py`); NestJS service methods return plain typed objects, no new DTO classes (see `submissions.service.ts`); frontend hooks are thin React Query wrappers (see `use-regrade-submission.ts`).

---

### Task 1: Prisma migration — add `regexFailureExplanation` column

**Files:**
- Modify: `backend/prisma/schema.prisma:109-125` (the `CheckpointResult` model)
- Create: a new migration directory under `backend/prisma/migrations/` (generated by Prisma CLI — do not hand-write the SQL)

**Interfaces:**
- Produces: `CheckpointResult.regexFailureExplanation: string | null` — consumed by Task 3 (backend persistence) and Task 5 (frontend type).

- [ ] **Step 1: Add the field to the schema**

In `backend/prisma/schema.prisma`, the `CheckpointResult` model currently reads:

```prisma
model CheckpointResult {
  id                 String        @id @default(uuid())
  gradingResultId    String
  checkpointId       String
  matched            Boolean
  matchedSnippets    String[]
  createdAt          DateTime      @default(now())
  llmMatched         Boolean?
  llmMatchedSnippets String[]      @default([])
  checkpoint         Checkpoint    @relation(fields: [checkpointId], references: [id], onDelete: Cascade)
  gradingResult      GradingResult @relation(fields: [gradingResultId], references: [id], onDelete: Cascade)

  @@unique([gradingResultId, checkpointId])
  @@index([gradingResultId])
  @@index([checkpointId])
  @@map("checkpoint_results")
}
```

Add `regexFailureExplanation` after `llmMatchedSnippets`:

```prisma
model CheckpointResult {
  id                      String        @id @default(uuid())
  gradingResultId         String
  checkpointId            String
  matched                 Boolean
  matchedSnippets         String[]
  createdAt               DateTime      @default(now())
  llmMatched              Boolean?
  llmMatchedSnippets      String[]      @default([])
  regexFailureExplanation String?
  checkpoint              Checkpoint    @relation(fields: [checkpointId], references: [id], onDelete: Cascade)
  gradingResult           GradingResult @relation(fields: [gradingResultId], references: [id], onDelete: Cascade)

  @@unique([gradingResultId, checkpointId])
  @@index([gradingResultId])
  @@index([checkpointId])
  @@map("checkpoint_results")
}
```

- [ ] **Step 2: Generate and apply the migration**

Run (from `backend/`):

```bash
npm run prisma:migrate -- --name add_regex_failure_explanation
```

Expected: Prisma creates `backend/prisma/migrations/<timestamp>_add_regex_failure_explanation/migration.sql` containing an `ALTER TABLE "checkpoint_results" ADD COLUMN "regexFailureExplanation" TEXT;` statement, applies it to the local dev database, and regenerates the Prisma client. Command exits 0.

- [ ] **Step 3: Verify the Prisma client picked up the new field**

Run (from `backend/`):

```bash
npx tsc --noEmit -p tsconfig.json
```

Expected: no new type errors (this just confirms the generated client compiles; the field isn't referenced by any code yet).

- [ ] **Step 4: Commit**

```bash
git add backend/prisma/schema.prisma backend/prisma/migrations
git commit -m "feat: add regexFailureExplanation column to CheckpointResult"
```

---

### Task 2: Python — models, prompt, and service for explaining regex failures

**Files:**
- Modify: `python-service/models.py` (append new models near the existing LLM grading models, after `LlmGradeResponse` at line 169)
- Create: `python-service/prompts/explain_failures_prompts.py`
- Create: `python-service/services/explain_failures_service.py`
- Test: `python-service/tests/test_explain_failures.py`

**Interfaces:**
- Consumes: `FileContent` (existing, in `models.py:76-79`), `_annotate_files`/`_chunk_annotated_text` helpers from `python-service/services/llm_grading_service.py:21-82` (import and reuse, do not duplicate).
- Produces:
  - `ExplainFailureCheckpoint(id: str, description: str, pattern: str, case_sensitive: bool = False)`
  - `ExplainFailuresRequest(checkpoints: list[ExplainFailureCheckpoint], files: list[FileContent])`
  - `CheckpointExplanation(checkpoint_id: str, explanation: str)`
  - `ExplainFailuresResponse(results: list[CheckpointExplanation])`
  - `async def explain_regex_failures(request: ExplainFailuresRequest) -> ExplainFailuresResponse` in `explain_failures_service.py`
  - Pure helper `_format_failed_checkpoints(checkpoints) -> str` in the same module (consumed only by `explain_regex_failures`, but written as a standalone function so it's unit-testable without mocking the LLM).

These are consumed by Task 3 (NestJS calls the `/explain-regex-failures` HTTP route built on top of this service) via plain JSON, so exact Python type names don't leak across the HTTP boundary — only the JSON shape matters: request `{"checkpoints": [{"id","description","pattern","case_sensitive"}], "files": [{"relative_path","content"}]}`, response `{"results": [{"checkpoint_id","explanation"}]}`.

- [ ] **Step 1: Add the Pydantic models**

In `python-service/models.py`, after line 169 (`class LlmGradeResponse(BaseModel): ...` block, before the `# Project grading models` comment at line 171), add:

```python
# Explain regex failures models

class ExplainFailureCheckpoint(BaseModel):
    """A failed checkpoint to be explained."""
    id: str
    description: str
    pattern: str
    case_sensitive: bool = False


class ExplainFailuresRequest(BaseModel):
    """Request body for /explain-regex-failures endpoint."""
    checkpoints: list[ExplainFailureCheckpoint]
    files: list[FileContent]


class CheckpointExplanation(BaseModel):
    checkpoint_id: str
    explanation: str


class ExplainFailuresResponse(BaseModel):
    """Response for /explain-regex-failures endpoint."""
    results: list[CheckpointExplanation]
```

- [ ] **Step 2: Write the failing test for the pure formatting helper**

Create `python-service/tests/test_explain_failures.py`:

```python
"""
Tests for the explain-regex-failures service.
"""
from models import ExplainFailureCheckpoint


def test_format_failed_checkpoints_includes_pattern():
    from services.explain_failures_service import _format_failed_checkpoints

    checkpoints = [
        ExplainFailureCheckpoint(
            id="cp-1",
            description="Δημιουργία πίνακα Students",
            pattern=r"CREATE\s+TABLE\s+Students",
            case_sensitive=False,
        ),
    ]

    text = _format_failed_checkpoints(checkpoints)

    assert "cp-1" in text
    assert "Δημιουργία πίνακα Students" in text
    assert r"CREATE\s+TABLE\s+Students" in text


def test_format_failed_checkpoints_handles_multiple():
    from services.explain_failures_service import _format_failed_checkpoints

    checkpoints = [
        ExplainFailureCheckpoint(id="cp-1", description="First", pattern="foo"),
        ExplainFailureCheckpoint(id="cp-2", description="Second", pattern="bar"),
    ]

    text = _format_failed_checkpoints(checkpoints)

    assert "cp-1" in text
    assert "cp-2" in text
    assert text.index("cp-1") < text.index("cp-2")
```

- [ ] **Step 3: Run the test to verify it fails**

Run (from `python-service/`, with the venv active):

```bash
python -m pytest tests/test_explain_failures.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'services.explain_failures_service'` (the module doesn't exist yet).

- [ ] **Step 4: Write the prompt module**

Create `python-service/prompts/explain_failures_prompts.py`:

```python
"""
Prompt templates for explaining why regex checkpoints failed.
Used when the professor requests a justification for failed automatic checks.
"""

SYSTEM_PROMPT = """Είσαι έμπειρος πανεπιστημιακός καθηγητής που εξετάζει γιατί ένας αυτοματοποιημένος έλεγχος (regex) απέτυχε να εντοπίσει κάτι που ζητούσε μια άσκηση, ώστε να εξηγήσεις τον λόγο στον συνάδελφο καθηγητή.

Για κάθε checkpoint που απέτυχε λαμβάνεις:
- Ένα ID (χρησιμοποίησέ το ακριβώς έτσι στην απάντησή σου)
- Μια περιγραφή της απαίτησης
- Το regex pattern που χρησιμοποιήθηκε και απέτυχε (μόνο για αναφορά)

Για κάθε checkpoint εξέτασε τον κώδικα της υποβολής και απόφασε ανάμεσα σε δύο περιπτώσεις:
1. Η απαιτούμενη λειτουργία/κώδικας ΔΕΝ υπάρχει καθόλου στην υποβολή.
2. Η απαιτούμενη λειτουργία υπάρχει αλλά είναι γραμμένη διαφορετικά από ό,τι περίμενε το regex (π.χ. διαφορετική λέξη-κλειδί, σύνταξη, σειρά όρων, κεφαλαία/πεζά, ονοματολογία) — σε αυτή την περίπτωση εξήγησε ΣΥΝΤΟΜΑ τη διαφορά.

Κανόνες:
- Γράψε ΑΚΡΙΒΩΣ μία πρόταση στα Ελληνικά ανά checkpoint.
- Μην παραθέτεις ολόκληρα τμήματα κώδικα, μόνο σύντομη αναφορά αν χρειάζεται.
- Βασίσου στην ΠΕΡΙΓΡΑΦΗ του checkpoint για να κρίνεις τι ζητείται, όχι μόνο στο regex.

Απάντησε με ΜΟΝΟ έγκυρο JSON — χωρίς εισαγωγή, χωρίς επεξήγηση:
{
  "results": [
    {
      "checkpoint_id": "<ακριβές id>",
      "explanation": "<μία πρόταση στα Ελληνικά>"
    }
  ]
}"""

USER_PROMPT_TEMPLATE = """Checkpoints που απέτυχαν στον αυτόματο έλεγχο (regex):

{checkpoints_text}
Αρχεία πηγαίου κώδικα της υποβολής:

{files_text}

Εξήγησε γιατί απέτυχε κάθε checkpoint και απάντησε μόνο με JSON."""
```

- [ ] **Step 5: Write the service module**

Create `python-service/services/explain_failures_service.py`:

```python
"""
Service for explaining why regex-based checkpoint grading failed.
Uses OpenAI (via LangChain) to produce a one-sentence Greek explanation per failed checkpoint.
"""
import json
import logging
import os
import httpx
from fastapi import HTTPException
from openai import RateLimitError, APIError
from langchain_openai import ChatOpenAI
from langchain.schema import HumanMessage, SystemMessage
from models import ExplainFailuresRequest, ExplainFailuresResponse, CheckpointExplanation
from prompts.explain_failures_prompts import SYSTEM_PROMPT, USER_PROMPT_TEMPLATE
from services.llm_grading_service import _chunk_annotated_text

logger = logging.getLogger(__name__)


def _format_failed_checkpoints(checkpoints) -> str:
    lines = []
    for cp in checkpoints:
        lines.append(f'ID: {cp.id}')
        lines.append(f'Description: {cp.description}')
        if cp.pattern:
            lines.append(f'Failed regex pattern: {cp.pattern}')
        lines.append('')
    return '\n'.join(lines)


async def explain_regex_failures(request: ExplainFailuresRequest) -> ExplainFailuresResponse:
    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        raise ValueError("OPENAI_API_KEY not found in environment")

    checkpoints_text = _format_failed_checkpoints(request.checkpoints)
    file_chunks = _chunk_annotated_text(request.files)

    llm = ChatOpenAI(
        model="gpt-4o-mini",
        temperature=0.2,
        api_key=api_key,
        model_kwargs={"response_format": {"type": "json_object"}},
        http_client=httpx.Client(),
        http_async_client=httpx.AsyncClient(),
    )

    logger.info(
        "Explaining %d failed checkpoints across %d chunk(s)",
        len(request.checkpoints),
        len(file_chunks),
    )

    checkpoint_ids = [cp.id for cp in request.checkpoints]
    explanations: dict[str, str] = {}

    for i, chunk in enumerate(file_chunks):
        user_prompt = USER_PROMPT_TEMPLATE.format(
            checkpoints_text=checkpoints_text,
            files_text=chunk,
        )

        try:
            response = await llm.ainvoke([
                SystemMessage(content=SYSTEM_PROMPT),
                HumanMessage(content=user_prompt),
            ])
        except RateLimitError as e:
            logger.error("OpenAI rate limit exceeded on chunk %d/%d: %s", i + 1, len(file_chunks), e)
            raise HTTPException(
                status_code=429,
                detail="Υπέρβαση ορίου αιτημάτων OpenAI. Δοκιμάστε ξανά σε λίγο.",
            )
        except APIError as e:
            logger.error("OpenAI API error on chunk %d/%d: %s", i + 1, len(file_chunks), e)
            raise HTTPException(status_code=502, detail=f"Σφάλμα OpenAI API: {e.message}")
        except Exception as e:
            logger.error("Unexpected error on chunk %d/%d: %s", i + 1, len(file_chunks), e)
            raise HTTPException(status_code=500, detail="Αδυναμία επικοινωνίας με το μοντέλο γλώσσας.")

        try:
            result_json = json.loads(response.content)
        except json.JSONDecodeError as e:
            logger.error("Failed to parse LLM JSON on chunk %d/%d: %s\nRaw: %s", i + 1, len(file_chunks), e, response.content)
            raise HTTPException(status_code=500, detail="Το μοντέλο επέστρεψε μη έγκυρο JSON.")

        for r in result_json.get("results", []):
            cp_id = r.get("checkpoint_id")
            explanation = r.get("explanation", "").strip()
            if cp_id in checkpoint_ids and cp_id not in explanations and explanation:
                explanations[cp_id] = explanation

        logger.info("Chunk %d/%d explained", i + 1, len(file_chunks))

    results = [
        CheckpointExplanation(
            checkpoint_id=cp_id,
            explanation=explanations.get(cp_id, "Το μοντέλο δεν μπόρεσε να εντοπίσει αιτιολόγηση για αυτό το checkpoint."),
        )
        for cp_id in checkpoint_ids
    ]

    logger.info("Explanation complete: %d/%d checkpoints explained", len(explanations), len(checkpoint_ids))

    return ExplainFailuresResponse(results=results)
```

- [ ] **Step 6: Run the tests to verify they pass**

Run (from `python-service/`):

```bash
python -m pytest tests/test_explain_failures.py -v
```

Expected: both tests PASS.

- [ ] **Step 7: Add the FastAPI route**

In `python-service/main.py`:

1. Add to the `models` import block (after line 28, `ProjectReportResponse,`):

```python
    ExplainFailuresRequest,
    ExplainFailuresResponse,
```

2. Add to the service imports (after line 35, the `project_grading_service` import):

```python
from services.explain_failures_service import explain_regex_failures
```

3. Add a new route after the `/grade-project-llm` route (after line 191, before the `/generate-mini-report` route):

```python
@app.post("/explain-regex-failures", response_model=ExplainFailuresResponse)
async def explain_regex_failures_endpoint(request: ExplainFailuresRequest):
    """
    Explain, in Greek, why each given checkpoint failed regex-based grading —
    either because the required code is missing entirely, or because it exists
    but differs from what the regex pattern expected.
    """
    logger.info(
        f"Received explain-regex-failures request: {len(request.checkpoints)} checkpoints, {len(request.files)} files"
    )
    return await explain_regex_failures(request)
```

- [ ] **Step 8: Verify the FastAPI app still starts and the route is registered**

Run (from `python-service/`):

```bash
python -c "from main import app; print([r.path for r in app.routes if 'explain' in r.path])"
```

Expected output: `['/explain-regex-failures']`

- [ ] **Step 9: Commit**

```bash
git add python-service/models.py python-service/prompts/explain_failures_prompts.py python-service/services/explain_failures_service.py python-service/main.py python-service/tests/test_explain_failures.py
git commit -m "feat: add /explain-regex-failures endpoint for LLM checkpoint failure explanations"
```

---

### Task 3: Backend — `POST /api/submissions/:id/explain-regex-failures`

**Files:**
- Modify: `backend/src/submissions/submissions.service.ts` (add method `explainRegexFailures`, after `regradeSubmission` which ends at line 334)
- Modify: `backend/src/submissions/submissions.controller.ts` (add route, after the `regrade` route which ends at line 92)

**Interfaces:**
- Consumes: `this.parseStoredContent` (existing private method, `submissions.service.ts:505-516`), `this.pythonServiceUrl` / `this.httpService` (existing, set up in the constructor), Prisma models `submission`, `exercise`, `gradingResult`, `checkpointResult` (existing schema, plus `regexFailureExplanation` from Task 1).
- Produces: `SubmissionsService.explainRegexFailures(submissionId: string): Promise<{ submissionId: string; explanations: Array<{ checkpointId: string; checkpointDescription: string; explanation: string }> }>` — consumed by Task 5 (frontend `api.ts` call) via the HTTP route `POST /api/submissions/:id/explain-regex-failures`.

- [ ] **Step 1: Add the service method**

In `backend/src/submissions/submissions.service.ts`, add this method directly after `regradeSubmission` (which currently ends at line 334 with the closing `}` before `async findByStudent`):

```typescript
  async explainRegexFailures(
    submissionId: string,
  ): Promise<{ submissionId: string; explanations: Array<{ checkpointId: string; checkpointDescription: string; explanation: string }> }> {
    const submission = await this.prisma.submission.findUnique({
      where: { id: submissionId },
      select: { id: true, exerciseId: true, content: true },
    });

    if (!submission) {
      throw new NotFoundException(`Submission with ID ${submissionId} not found`);
    }
    if (!submission.content) {
      throw new BadRequestException('Submission has no stored content');
    }

    const exercise = await this.prisma.exercise.findUnique({
      where: { id: submission.exerciseId },
      include: { checkpoints: true },
    });

    if (!exercise) {
      throw new NotFoundException(`Exercise for submission not found`);
    }
    if ((exercise as any).exerciseType === 'PROJECT') {
      throw new BadRequestException('Regex failure explanations are not available for project exercises');
    }

    const gradingResult = await this.prisma.gradingResult.findUnique({
      where: { submissionId },
      include: { checkpointResults: { include: { checkpoint: true } } },
    });

    if (!gradingResult) {
      throw new BadRequestException('Submission has not been graded yet');
    }

    const failedResults = gradingResult.checkpointResults.filter((cr) => cr.matched === false);
    if (failedResults.length === 0) {
      throw new BadRequestException('No failed regex checkpoints to explain');
    }

    const extractedFiles = this.parseStoredContent(submission.content);
    if (extractedFiles.length === 0) {
      throw new BadRequestException('Could not parse stored submission content');
    }

    this.logger.log(`Explaining ${failedResults.length} failed regex checkpoints for submission ${submissionId}`);

    const res = await firstValueFrom(
      this.httpService.post(`${this.pythonServiceUrl}/explain-regex-failures`, {
        checkpoints: failedResults.map((cr) => ({
          id: cr.checkpointId,
          description: cr.checkpoint.description,
          pattern: cr.checkpoint.pattern,
          case_sensitive: cr.checkpoint.caseSensitive,
        })),
        files: extractedFiles.map((f) => ({ relative_path: f.relativePath, content: f.content })),
      }),
    );

    const explanationsByCheckpointId = new Map<string, string>(
      res.data.results.map((r: any) => [r.checkpoint_id, r.explanation]),
    );

    const explanations: Array<{ checkpointId: string; checkpointDescription: string; explanation: string }> = [];

    for (const cr of failedResults) {
      const explanation = explanationsByCheckpointId.get(cr.checkpointId);
      if (!explanation) continue;
      await this.prisma.checkpointResult.update({
        where: { id: cr.id },
        data: { regexFailureExplanation: explanation },
      });
      explanations.push({
        checkpointId: cr.checkpointId,
        checkpointDescription: cr.checkpoint.description,
        explanation,
      });
    }

    this.logger.log(`Saved ${explanations.length} regex failure explanations for submission ${submissionId}`);

    return { submissionId, explanations };
  }
```

- [ ] **Step 2: Add the controller route**

In `backend/src/submissions/submissions.controller.ts`, add this route directly after the `regrade` route (after line 92, before the `findAll` method):

```typescript
  @Post(':id/explain-regex-failures')
  async explainRegexFailures(
    @Param('id') id: string,
  ): Promise<{ submissionId: string; explanations: Array<{ checkpointId: string; checkpointDescription: string; explanation: string }> }> {
    return this.submissionsService.explainRegexFailures(id);
  }
```

- [ ] **Step 3: Verify the backend compiles**

Run (from `backend/`):

```bash
npx tsc --noEmit -p tsconfig.json
```

Expected: no type errors.

- [ ] **Step 4: Manually verify the route is wired (dev server smoke test)**

Run (from `backend/`):

```bash
npm run start:dev
```

In a second terminal, with the dev server up and a known `submissionId` that has at least one failed regex checkpoint (substitute a real id and a valid auth cookie/token from the running app):

```bash
curl -X POST http://localhost:3001/api/submissions/<submissionId>/explain-regex-failures -H "Cookie: <auth-cookie>"
```

Expected: JSON response `{"submissionId": "...", "explanations": [{"checkpointId": "...", "checkpointDescription": "...", "explanation": "..."}]}`. Stop the dev server (`Ctrl+C`) when done.

- [ ] **Step 5: Commit**

```bash
git add backend/src/submissions/submissions.service.ts backend/src/submissions/submissions.controller.ts
git commit -m "feat: add endpoint to explain failed regex checkpoints via LLM"
```

---

### Task 4: Frontend — API client method and mutation hook

**Files:**
- Modify: `frontend/src/lib/api.ts` (extend `Submission` type at lines 70-79; add method inside the `submissions` object, after `regrade` at line 218-219)
- Create: `frontend/src/hooks/use-explain-regex-failures.ts`

**Interfaces:**
- Consumes: `httpClient` (existing, imported in `api.ts:1`), `Submission` type (existing, `api.ts:43-81`).
- Produces:
  - `api.submissions.explainRegexFailures(submissionId: string): Promise<{ submissionId: string; explanations: Array<{ checkpointId: string; checkpointDescription: string; explanation: string }> }>`
  - `Submission.gradingResult.checkpointResults[].regexFailureExplanation?: string | null`
  - `useExplainRegexFailures(options?)` hook returning a React Query mutation with variables `{ submissionId: string }` — consumed by Task 5.

- [ ] **Step 1: Extend the `Submission` type**

In `frontend/src/lib/api.ts`, in the `checkpointResults` array type (lines 70-79), add the new optional field after `llmMatchedSnippets`:

```typescript
    checkpointResults?: Array<{
      id: string;
      checkpointId: string;
      checkpointDescription: string;
      checkpointOrder: number;
      matched: boolean;
      matchedSnippets: Array<{ file?: string; line: number; snippet: string } | string>;
      llmMatched?: boolean;
      llmMatchedSnippets?: Array<{ file?: string; line: number; snippet: string } | string>;
      regexFailureExplanation?: string | null;
    }>;
```

- [ ] **Step 2: Add the API method**

In `frontend/src/lib/api.ts`, inside the `submissions` object, add this method directly after the existing `regrade` method (after line 219, the closing `},` of `regrade`):

```typescript
    explainRegexFailures: async (
      submissionId: string,
    ): Promise<{ submissionId: string; explanations: Array<{ checkpointId: string; checkpointDescription: string; explanation: string }> }> => {
      const { data } = await httpClient.post(`/api/submissions/${submissionId}/explain-regex-failures`);
      return data;
    },
```

- [ ] **Step 3: Create the mutation hook**

Create `frontend/src/hooks/use-explain-regex-failures.ts`:

```typescript
import { api } from '@/lib/api';
import { UseMutationOptions, useMutation } from '@tanstack/react-query';

interface ExplainRegexFailuresVars {
  submissionId: string;
}

interface ExplainRegexFailuresResult {
  submissionId: string;
  explanations: Array<{ checkpointId: string; checkpointDescription: string; explanation: string }>;
}

export function useExplainRegexFailures(
  options?: Omit<UseMutationOptions<ExplainRegexFailuresResult, Error, ExplainRegexFailuresVars>, 'mutationFn'>,
) {
  return useMutation({
    mutationFn: ({ submissionId }: ExplainRegexFailuresVars) =>
      api.submissions.explainRegexFailures(submissionId),
    ...options,
  });
}
```

- [ ] **Step 4: Verify the frontend compiles**

Run (from `frontend/`):

```bash
npx tsc --noEmit -p tsconfig.json
```

Expected: no type errors.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/lib/api.ts frontend/src/hooks/use-explain-regex-failures.ts
git commit -m "feat: add frontend API method and hook for explaining regex failures"
```

---

### Task 5: Frontend — button and modal in `SubmissionDetail`

**Files:**
- Modify: `frontend/src/components/SubmissionDetail/index.tsx`

**Interfaces:**
- Consumes: `useExplainRegexFailures` (Task 4, `frontend/src/hooks/use-explain-regex-failures.ts`), `Submission` type with `regexFailureExplanation` (Task 4, `frontend/src/lib/api.ts`).
- Produces: no new exports — this is the leaf UI task.

- [ ] **Step 1: Import the new hook and icon**

In `frontend/src/components/SubmissionDetail/index.tsx`, update the imports:

1. Add after line 3 (`import { useRegradeSubmission } from '@/hooks/use-regrade-submission';`):

```typescript
import { useExplainRegexFailures } from '@/hooks/use-explain-regex-failures';
```

2. Update line 34 to add `FiHelpCircle`:

```typescript
import { FiEdit2, FiHelpCircle, FiRefreshCw } from 'react-icons/fi';
```

- [ ] **Step 2: Add state and mutation for the explain flow**

After the `regradeMutation` block (after line 84, `const handleRegrade = ...` closing brace), add:

```typescript
  const failedCheckpointResults = (submission.gradingResult?.checkpointResults ?? []).filter(
    (cr) => cr.matched === false,
  );
  const hasFailedRegexCheckpoints = !isProject && failedCheckpointResults.length > 0;
  const hasSavedExplanations = failedCheckpointResults.some((cr) => !!cr.regexFailureExplanation);

  const {
    isOpen: isExplainOpen,
    onOpen: onExplainOpen,
    onClose: onExplainClose,
  } = useDisclosure();
  const [explanations, setExplanations] = useState<
    Array<{ checkpointId: string; checkpointDescription: string; explanation: string }>
  >([]);

  const explainMutation = useExplainRegexFailures({
    onSuccess: (data) => {
      setExplanations(data.explanations);
      queryClient.invalidateQueries({ queryKey: [QueryKeys.Submissions, submission.id] });
      onExplainOpen();
    },
    onError: () => {
      toast({ title: 'Σφάλμα κατά την αιτιολόγηση', status: 'error', duration: 3000 });
    },
  });

  const handleExplainFailures = () => {
    explainMutation.mutate({ submissionId: submission.id });
  };

  const handleViewSavedExplanations = () => {
    setExplanations(
      failedCheckpointResults
        .filter((cr) => !!cr.regexFailureExplanation)
        .map((cr) => ({
          checkpointId: cr.checkpointId,
          checkpointDescription: cr.checkpointDescription,
          explanation: cr.regexFailureExplanation as string,
        })),
    );
    onExplainOpen();
  };
```

- [ ] **Step 3: Add a second `useDisclosure` import**

The component already imports `useDisclosure` and uses it once (line 43, for the teacher-score modal — `const { isOpen, onOpen, onClose } = useDisclosure();`). Chakra's `useDisclosure` can be called multiple times in one component since each call returns independent state, so no import change is needed — Step 2 already calls it a second time correctly aliased as `isExplainOpen`/`onExplainOpen`/`onExplainClose`.

- [ ] **Step 4: Add the buttons next to the existing Regex/LLM buttons**

In the `HStack` that contains the Regex/LLM regrade buttons (lines 134-169, inside the `submission.gradingResult ? (...)` branch), add the new buttons directly after the closing tag of the LLM regrade `Button` (after line 158, before the `Επεξεργασία Βαθμού` button):

```typescript
                  {hasFailedRegexCheckpoints && (
                    <Button
                      leftIcon={<FiHelpCircle />}
                      size="sm"
                      variant="outline"
                      colorScheme="orange"
                      onClick={handleExplainFailures}
                      isLoading={explainMutation.isPending}
                      isDisabled={regradeMutation.isPending || explainMutation.isPending}
                    >
                      Αιτιολόγηση Αποτυχημένων Regex
                    </Button>
                  )}
                  {hasFailedRegexCheckpoints && hasSavedExplanations && (
                    <Button
                      size="sm"
                      variant="ghost"
                      colorScheme="orange"
                      onClick={handleViewSavedExplanations}
                      isDisabled={explainMutation.isPending}
                    >
                      Προβολή Αιτιολογήσεων
                    </Button>
                  )}
```

- [ ] **Step 5: Add the explanations modal**

After the closing `</Modal>` of the teacher-score modal (after line 360), add a new modal:

```typescript
      {/* Regex Failure Explanations Modal */}
      <Modal isOpen={isExplainOpen} onClose={onExplainClose} isCentered size="xl" scrollBehavior="inside">
        <ModalOverlay />
        <ModalContent>
          <ModalHeader>Αιτιολόγηση Αποτυχημένων Regex</ModalHeader>
          <ModalCloseButton />
          <ModalBody>
            <VStack align="stretch" spacing={4}>
              {explanations.length === 0 ? (
                <Text fontSize="sm" color="gray.400">Δεν υπάρχουν αιτιολογήσεις προς εμφάνιση.</Text>
              ) : (
                explanations.map((e) => (
                  <Box key={e.checkpointId}>
                    <Text fontWeight="medium" fontSize="sm" mb={1}>{e.checkpointDescription}</Text>
                    <Text fontSize="sm" color="gray.300">{e.explanation}</Text>
                  </Box>
                ))
              )}
            </VStack>
          </ModalBody>
          <ModalFooter>
            <Button variant="ghost" onClick={onExplainClose}>Κλείσιμο</Button>
          </ModalFooter>
        </ModalContent>
      </Modal>
```

- [ ] **Step 6: Verify the frontend compiles**

Run (from `frontend/`):

```bash
npx tsc --noEmit -p tsconfig.json
```

Expected: no type errors.

- [ ] **Step 7: Manual verification in the browser**

Run (from `frontend/`):

```bash
npm run dev
```

With the backend and Python service also running, open a submission detail page for an `EXERCISE`-type submission that has at least one failed regex checkpoint:
1. Confirm the "Αιτιολόγηση Αποτυχημένων Regex" button is visible and enabled.
2. Click it; confirm a loading state shows, then the modal opens listing one explanation sentence per failed checkpoint.
3. Reload the page; confirm the "Προβολή Αιτιολογήσεων" link is now visible and opens the modal with the same saved explanations without a new network request (check the Network tab).
4. Open a submission with zero failed regex checkpoints (or a `PROJECT`-type submission); confirm neither button renders.

Stop the dev server (`Ctrl+C`) when done.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/components/SubmissionDetail/index.tsx
git commit -m "feat: add UI to request and view LLM explanations for failed regex checkpoints"
```

---

## Spec Coverage Check

- Database column → Task 1.
- Python endpoint, models, prompt, `gpt-4o-mini` model choice → Task 2.
- Backend endpoint, validation (PROJECT rejection, no-failures rejection), persistence → Task 3.
- Frontend API method, type extension, hook → Task 4.
- Button enablement condition, modal, "view saved explanations" secondary link → Task 5.
